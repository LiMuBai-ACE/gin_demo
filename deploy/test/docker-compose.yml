version: '3.4'

services:
  weatherinfos:
    image: gin_demo
    build:
      context: ../..
      dockerfile: ./Dockerfile
    ports:
      - 4000:4000
    environment:
      ENV: test # production develop
